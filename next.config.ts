/** @type {import('next').NextConfig} */

const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "media.istockphoto.com",
      // add more domains as needed, e.g. "images.unsplash.com"
    ],
  },
};

export default nextConfig;
